version: 2
updates:
  # This tells Dependabot to check your GitHub Actions (workflows)
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly" # Check for updates once a week (Monday morning)
    open-pull-requests-limit: 5 # Don't overwhelm me; only 5 suggestions at a time